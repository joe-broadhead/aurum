---
name: Bug report
about: Something broken in Aurum CLI or aurum-core
labels: bug
---

## Environment
- OS / arch:
- Aurum version (`aurum --version`):
- Provider / model / profile:
- ffmpeg present? (`ffmpeg -version`):
- Support bundle attached? (`aurum support-bundle`) — **preferred**

## Command
```bash
# redact secrets; never paste API keys
```

## Expected

## Actual

## Diagnostics
- Prefer attaching `aurum support-bundle` JSON (no audio/transcripts/keys).
- Optional: `aurum doctor --json` excerpt.

## Logs
(`-v` stderr; **no API keys, no audio, no full transcripts**)
