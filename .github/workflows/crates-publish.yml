name: Publish crates.io

# Manual only. Does not run on tag/push.
# Order: aurum-core first, then optional aurum CLI crate.
on:
  workflow_dispatch:
    inputs:
      crate:
        description: "Which crate to publish"
        required: true
        type: choice
        options:
          - aurum-core
          - aurum
      dry_run:
        description: "Dry run (no upload)"
        required: true
        type: boolean
        default: true

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    environment: crates-io
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@stable

      - name: Install build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake pkg-config

      - name: Version gate
        run: |
          chmod +x scripts/version_check.sh
          ./scripts/version_check.sh

      - name: Test gate
        run: cargo test --workspace --locked

      - name: Publish
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
          CRATE: ${{ inputs.crate }}
          DRY_RUN: ${{ inputs.dry_run }}
        run: |
          set -euo pipefail
          if [ -z "${CARGO_REGISTRY_TOKEN:-}" ] && [ "${DRY_RUN}" != "true" ]; then
            echo "CARGO_REGISTRY_TOKEN secret is required for real publish"
            echo "Add it under Settings → Environments → crates-io (or repo secrets)."
            exit 1
          fi
          args=(-p "${CRATE}" --locked)
          if [ "${DRY_RUN}" = "true" ]; then
            args+=(--dry-run)
          fi
          # aurum CLI needs aurum-core already on crates.io
          cargo publish "${args[@]}"
